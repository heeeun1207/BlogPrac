import styled from "styled-components";

/**
 *회원가입 또는 로그인 폼을 보여준다.
 */

const AuthFormBlok = styled.div``;

const AuthForm = () => {
  return (
    <AuthFormBlok>
      AuthForm
    </AuthFormBlok>
  );
};

export default AuthForm;
